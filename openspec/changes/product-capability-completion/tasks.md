# 实施任务

## 1. 差距与契约

- [x] 建立产品功能、前端 API 与后端 controller 的可重复契约审计。
- [x] 补齐简历模型、DTO、用户所有权 CRUD 和前端运行时 OSS 配置。
- [x] 补齐三类历史、结果详情、未完成面试、问答结果和会话历史接口。
- [x] 补齐管理员面试人数聚合并修正用户更新等现有前后端契约。
- [x] 增加基础业务闭环的权限、分页、恢复和错误路径测试。

## 2. 支付与权益

- [x] 定义服务端套餐目录、支付 DTO、订单状态机、订单模型和交易流水模型。
- [x] 实现支付宝、微信和测试 provider 的创建、查询与回调验证边界。
- [x] 实现订单创建、主动查询、订单所有权、金额校验和幂等发货。
- [x] 实现用户交易记录、旺旺币原子兑换和一次性分享奖励。
- [x] 完成前端充值、轮询清理、兑换、记录刷新和失败/过期状态交互。
- [x] 增加支付重复通知、并发认领、金额篡改、越权、补偿和兑换透支测试。

## 3. AI 与可观测性扩展

- [x] 将长对话总结、最近消息窗口和过期会话定时清理接入真实调用链。
- [x] 实现用户隔离、可关闭、失败可降级的面试知识 RAG 检索边界。
- [x] 实现 TraceID、结构化 Winston 日志、递归脱敏和统一异常日志。
- [x] 实现低基数 HTTP、AI、数据库与业务指标及 `/metrics` 端点。
- [x] 增加摘要降级、RAG 隔离、日志脱敏和指标标签测试。
- [x] 实现多模型切换、重试、超时、Token/成本指标和高成本接口限流。
- [x] 实现报告反馈、人工复核申请和管理员复核状态闭环。

## 4. 生产交付资产

- [x] 增加完整但无敏感值的示例环境变量和启动时配置校验。
- [x] 增加健康检查、后端/前端容器构建和本地编排配置。
- [x] 增加 PM2、Nginx、Prometheus、Grafana 和告警规则示例。
- [x] 校验生产依赖、构建产物入口、容器配置和文档链接。

## 5. 总体验证

- [x] 对照最低能力契约检查模块、接口、MCP、指标和规范字段，建立可重复能力覆盖审计。

- [x] 运行服务端单元/E2E 测试、前端/MCP/服务端构建和契约审计。
- [x] 运行敏感信息扫描、`git diff --check` 与 `deliveryguard check`。
- [x] 记录真实外部联调、正式验收和生产发布仍缺少的证据，不虚构完成状态。
